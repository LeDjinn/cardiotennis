<p id="notice"><%= notice %></p>

<h1>Les Programmes pour les 34 séances </h1>
<div class='container'>
<table class='table'>
  <thead>
    <tr>
      
      <th scope='col'>Num</th>
      <th scope='col'>Theme</th>
      <th scope='col'>Objectif</th>
      
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @programs.each do |program| %>
      <tr>
        
        <td><%= program.num %></td>
        <td><%= program.themeg %></td>
        <td><%= program.objectif %></td>
        
        
        <td><%= link_to 'Voir', program, {:class=>'btn btn-warning'} %></td>
        <td><%= link_to 'MODIFIER', edit_program_path(program), {:class=>'btn btn-primary'} %></td>
        <td><%= link_to 'EFFACER', program, method: :delete, data: { confirm: 'Are you sure?' }, class:'btn btn-danger' %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
<br>

<%= link_to 'New Program', new_program_path, class:'btn btn-success' %>
<%= link_to 'Home', root_path, class:'btn btn-warning'%>
